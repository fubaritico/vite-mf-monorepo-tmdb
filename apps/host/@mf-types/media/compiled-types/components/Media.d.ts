import type { FC } from 'react';
import '../remote.css';
/** Media detail page — composes hero, synopsis, photos, cast, trailers, similar and recommendations. */
declare const Media: FC;
export default Media;
