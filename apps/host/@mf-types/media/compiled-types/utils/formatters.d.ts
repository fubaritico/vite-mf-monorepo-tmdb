/**
 * Format runtime in minutes to hours and minutes
 * @param minutes - Runtime in minutes
 * @returns Formatted string (e.g., "2h 30m")
 */
export declare const formatRuntime: (minutes: number) => string;
