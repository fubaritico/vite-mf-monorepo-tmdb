import type { FC } from 'react';
import '../remote.css';
/** Search results page — displays movies, TV shows, actors and directors matching the query. */
declare const Search: FC;
export default Search;
